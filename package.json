{
  "dependencies": {
    "axios": "^1.5.1",
    "json-server": "^0.17.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.17.0",
    "react-scripts": "^5.0.1",
    "serve": "^14.2.1"
  },
  "scripts": {
    "start": "react-scripts start & json-server --watch db.json --port 3010",
    "build": "react-scripts build",
    "serve": "json-server --watch db.json --port 3010 & serve -s build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
